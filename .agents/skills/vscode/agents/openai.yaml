interface:
  display_name: "VS Code"
  short_description: "Repo-specific patterns and workflows for VS Code"
  default_prompt: "Use the vscode repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true